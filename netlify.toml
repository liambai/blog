[build]
  command = "pnpm build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  # Use Corepack so Netlify installs the exact pnpm from package.json's
  # `packageManager` field (the version verified locally), rather than its own.
  ENABLE_COREPACK = "true"
